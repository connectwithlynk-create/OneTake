# Clipnosis Landing Page

React + Tailwind landing page.

Install once:

```bash
cd landing-page
npm install
```

Run dev server:

```bash
npm run dev
```

Open URL printed by Vite, usually `http://localhost:5173`.

Build:

```bash
npm run build
```
